
# AI Call Analyzer Bot 🤖

Автоматизована система для аналізу дзвінків автосервісу за допомогою Google Gemini AI.
Бот скачує аудіо з Google Drive, транскрибує, аналізує по KPI та заносить результати в Google Sheets.

## 🚀 Функціонал
- **Транскрибація:** Перетворення аудіо в текст (українська мова).
- **KPI Аналіз:** Перевірка скриптів (Привітання, Кузов, Рік, Пробіг, Upsell).
- **Оцінка:** Виставлення балу менеджеру (1-10).
- **Звітність:** Заповнення Google Таблиці з кольоровим підсвічуванням проблемних зон.

## 🛠 Вимоги
- Python 3.10+
- Google Cloud Service Account (JSON ключ)
- Gemini API Key

## 📦 Встановлення та запуск (Локально)

1. **Клонуйте репозиторій:**
```bash
git clone https://github.com/nwrslept/CallAnalyzer.git
cd CallAnalyzer
 ```


2. **Створіть віртуальне середовище:**
```bash
python -m venv venv
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate
```

3. **Встановіть залежності:**
```bash
pip install -r requirements.txt
```


4. **Налаштування:**
* Перейменуйте `.env.example` в `.env` і додайте ваш `GEMINI_API_KEY`.
* Перевірте ID папок та таблиці у `.env`.
* Покладіть файл ключа `service_account.json` у корінь папки.


5. **Запуск:**
```bash
python main.py
```



